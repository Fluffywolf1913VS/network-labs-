# Commands - Lab 05

```text
ipconfig
ipconfig /renew
ping 192.168.1.18
```

Interpretation:

```text
169.254.x.x -> APIPA; DHCP failed
0.0.0.0     -> no IPv4 configuration yet
192.168.1.x -> valid lease from corrected DHCP scope
```
